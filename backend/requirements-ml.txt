# Необязательный слой: качественные эмбеддинги вместо hash-фоллбэка.
# Тянет torch (~1,5 ГБ) — ставьте, только когда поиск по смыслу реально нужен
# и на машине есть место. Контур работает и без этого файла.
-r requirements.txt
sentence-transformers>=3.0
